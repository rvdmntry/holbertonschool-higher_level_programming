-- List all records of the table second_table
-- This query retrieves all fields from all rows in second_table, ordered by score in descending order
SELECT score, name
FROM second_table
ORDER BY score DESC;
