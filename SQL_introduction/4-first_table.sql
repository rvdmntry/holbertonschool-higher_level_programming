-- Create the table first_table if it does not exist
-- This query creates a table named first_table with columns id and name
CREATE TABLE IF NOT EXISTS first_table (
    id INT,
    name VARCHAR(256)
);
